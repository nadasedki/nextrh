export declare function normalizeSkills(skills: string[]): string[];
export declare const isValidSkill: (s: string) => boolean;
export declare const skillMap: Record<string, string>;
export declare function normalizeSkillName(skill: string): string;
